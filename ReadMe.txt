To install Accessible maps:

1- Open the extensions from tools (or paste the url: chrome://extensions in the address bar

2- Enable the Developer mode

3- Click on "Load unpacked extension" and browse to AccessibleMaps folder

4- Open the "options" page to see the dynamic map

5- Open any other page. The globe button on the extension will be enabled when loading is finished